# Security notes

This plugin provides reversible encoding and message obfuscation. It does not provide cryptographic confidentiality.

Anyone with the shared 12-digit number can restore messages and create new messages that other clients accept. Web links stay visible to Discord and to people without the plugin. Discord also receives ordinary message metadata.

Do not use the plugin for passwords, account recovery codes, financial details, private keys, or anything that needs real encryption.
